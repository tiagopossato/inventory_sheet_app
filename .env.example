# Arquivo de configuração de ambiente (.env.example)
# Este arquivo serve como template para as variáveis de ambiente necessárias

## Configurações do Google Apps Script (CLASP)
# ID do projeto do Google Apps Script onde os arquivos processados serão implantados
CLASP_SCRIPT_ID=SCRIPT_ID

# Controle de implantação
# Quando definido como 1, solicita confirmação para criar nova versão de implantação
# Quando definido como 0, pula a confirmação de versão de implantação
ASK_FOR_DEPLOY=0

# ID da implantação da aplicação no Google Apps Script
DEPLOYMENT_ID=DEPLOYMENT_ID

## Configurações de teste
# ID da planilha mock para testes
MOCK_SPREADSHEET_ID=MOCK_SPREADSHEET_ID

## Credenciais de serviço do Google (Service Account)
## necessárias somente se você não adicionar o arquivo local_server/credentials.json
# Tipo de credencial do Google (geralmente "service_account")
GOOGLE_TYPE=GOOGLE_TYPE
# ID do projeto Google Cloud
GOOGLE_PROJECT_ID=GOOGLE_PROJECT_ID
# ID da chave privada da conta de serviço
GOOGLE_PRIVATE_KEY_ID=GOOGLE_PRIVATE_KEY_ID
# Chave privada da conta de serviço (formato PEM)
GOOGLE_PRIVATE_KEY=GOOGLE_PRIVATE_KEY
# Email da conta de serviço
GOOGLE_CLIENT_EMAIL=GOOGLE_CLIENT_EMAIL
# ID do cliente OAuth 2.0
GOOGLE_CLIENT_ID=GOOGLE_CLIENT_ID
# URI de autorização OAuth 2.0
GOOGLE_AUTH_URI=GOOGLE_AUTH_URI
# URI do endpoint de token OAuth 2.0
GOOGLE_TOKEN_URI=GOOGLE_TOKEN_URI
# URL do provedor de autenticação certificada x509
GOOGLE_AUTH_PROVIDER_URL=GOOGLE_AUTH_PROVIDER_URL
# URL do certificado do cliente
GOOGLE_CLIENT_CERT_URL=GOOGLE_CLIENT_CERT_URL
# Domínio do universo Google (geralmente "googleapis.com")
GOOGLE_UNIVERSE_DOMAIN=GOOGLE_UNIVERSE_DOMAIN